{
  "name": "gamedevjs-2021",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild client/index.js --bundle --minify --sourcemap --outfile=dist/app.js",
    "build:dev": "esbuild client/index.js --bundle --outfile=dist/app.js",
    "start": "node server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "GamedevJS"
  ],
  "author": "André Jaenisch",
  "license": "GPL-3.0+",
  "devDependencies": {
    "esbuild": "0.11.10",
    "gh-pages": "3.1.0"
  },
  "dependencies": {
    "fastify": "^3.14.2",
    "socket.io": "^4.0.1"
  }
}
