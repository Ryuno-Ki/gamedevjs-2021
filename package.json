{
  "name": "gamedevjs-2021",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild app=client/index.js index=client/index.html --loader:.html=file --bundle --minify --sourcemap --outdir=dist",
    "build:dev": "esbuild app=client/index.js index=client/index.html --loader:.html=file --bundle --outdir=dist",
    "start": "node server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "GamedevJS"
  ],
  "author": "André Jaenisch",
  "license": "GPL-3.0+",
  "devDependencies": {
    "esbuild": "0.11.11",
    "gh-pages": "3.1.0"
  },
  "dependencies": {
    "fastify": "^3.14.2",
    "fastify-static": "^4.0.1",
    "socket.io": "^4.0.1"
  },
  "engines": {
    "node": "15.14.0",
    "npm": "7.10.0"
  }
}
